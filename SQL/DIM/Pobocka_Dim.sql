create table Pobocka_DIM
(
	id_pobocka serial,
	mesto varchar(50),
	okres varchar(50),
	kraj varchar(50),
	adresa varchar(50),
	telefonne_cislo varchar(20),
	mail varchar(50),
	PRIMARY KEY (id_pobocka)
)